//TODO: Define appropriate table models, and establish table
//relationships through sequelize.
const { sequelize, Model, DataTypes} = require('./index.js');

module.exports.initializeSchema = () => {
  //Should return an object with keys and values corresponding to each
  //created table model.
}